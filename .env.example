# Environment configuration for hopfield-llm
# Copy to .env and modify as needed.

# Environment: "local" or "hpc" (auto-detected if not set)
# HOPFIELD_ENV=local

# Repository root (used by SLURM scripts)
# REPO_ROOT=/path/to/hopfield-llm

# HPC scratch directory for cached artifacts
# SCRATCH=/scratch/user
